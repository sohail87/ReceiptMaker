import React, { Component } from 'react'
import { connect } from 'react-redux'

export class ReceiptsViewSingle extends Component {
  render() {
    return (
      <div>
        ReceiptsViewSingle
      </div>
    )
  }
}

const mapStateToProps = (state) => ({
  
})

const mapDispatchToProps = {
  
}

export default connect(mapStateToProps, mapDispatchToProps)(ReceiptsViewSingle)
